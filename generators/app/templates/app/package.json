{
	"name": "",
	"version": "1.0.0",
	"scripts": {
		"start": "npm-run-all --parallel styles serve",
		"styles": "node-sass --watch styles:css --style compressed",
		"serve": "browser-sync start --server --files='**/*.*'"
	},
	"devDependencies": {
		"browser-sync": "^2.7.10",
		"node-sass": "^3.4.2",
		"npm-run-all": "^1.7.0"
	}
}
